// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { GoogleCaptchaGuard } from './google-captcha.guard';

describe('GoogleCaptchaGuard', () => {
  it('should be defined', () => {
    // expect(new GoogleCaptchaGuard()).toBeDefined();
  });
});
